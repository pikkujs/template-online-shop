// AUTO-GENERATED by @pikku/paraglide — do not edit.
// Source of truth: the `enum__*` keys in the message catalog,
// reconciled against the database enum columns.
import { m } from './messages.js'
import type { I18nString } from '@pikku/react'
import type { OrderStatus } from '../../../../packages/functions/.pikku/db/enums.gen.js'

/** A message accessor — call it at render time so the label tracks the active locale. */
export type I18nMessage = () => I18nString
/** A static, exhaustive map from an enum member to its branded i18n label. */
export type EnumLabel<E extends string> = Record<E, I18nMessage>

export const orderStatus = {
  pending: m.enum__order_status__pending,
  paid: m.enum__order_status__paid,
  shipped: m.enum__order_status__shipped,
  cancelled: m.enum__order_status__cancelled,
  payment_failed: m.enum__order_status__payment_failed,
  refunded: m.enum__order_status__refunded,
} satisfies EnumLabel<OrderStatus>
export type OrderStatusKey = keyof typeof orderStatus
